# Це файл, який Vercel читає автоматично для деплою — тому тут лише те, що
# реально потрібне в хмарі (провайдери "anthropic"/"azure"/"piper", Postgres-шар).
# ESPnet сюди й далі НЕ входить: разом з torch/stanza бандл на Vercel виходив
# за межу 500 МБ у 10 разів. Його ставлять окремо, за інструкцією з README.md.

# Ядро працює без залежностей — на заглушці (--llm mock) і в тестах.

# Справжня модель:
anthropic>=0.40.0

# Postgres-шар зберігання (app/storage/db.py) — потрібен лише коли
# STORAGE_BACKEND=postgres (задеплоєна версія на Vercel). Локально без цієї
# змінної db.py не імпортується взагалі, і психопг не обов'язковий.
psycopg2-binary>=2.9.9

# Piper — без torch (лише onnxruntime), тому, на відміну від ESPnet, влазить
# у бандл: сам пакет ~170 МБ + модель voices/piper/ (73 МБ) ≈ 250 МБ, разом з
# рештою все ще під новим лімітом Vercel для Python-функцій (500 МБ, 2026).
piper-tts>=1.7.0

# Наголоси (для tts_text.normalize) — лише marisa-trie: сам словник
# ukrainian-word-stress тут НЕ ставиться через pip (той затягнув би
# stanza/PyTorch — requirements.txt не вміє --no-deps для одного рядка), а
# вкопійований напряму в vendor/ (README.md там пояснює чому це безпечно).
# marisa-trie — лише 1,4 МБ і без важких залежностей, ставити нормально.
marisa-trie>=1.2.1
