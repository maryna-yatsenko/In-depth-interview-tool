# Ядро працює без залежностей — на заглушці (--llm mock) і в тестах.

# Справжня модель:
anthropic>=0.40.0

# Postgres-шар зберігання (app/storage/db.py) — потрібен лише коли
# STORAGE_BACKEND=postgres (задеплоєна версія на Vercel). Локально без цієї
# змінної db.py не імпортується взагалі, і психопг не обов'язковий — але
# тут, а не в окремому файлі, бо requirements.txt — єдиний, який Vercel
# читає автоматично.
psycopg2-binary>=2.9.9

# Приємний український голос локально й безкоштовно (три голоси, є чоловічий).
# Модель кладеться в models/ окремо, у git не їде (73 МБ) — див. README.
piper-tts>=1.7.0

# Наголоси — головний важіль української вимови: модель навчена їх читати.
# Ставити БЕЗ залежностей (--no-deps) + marisa-trie: інакше тягне stanza й
# PyTorch на ~2 ГБ, а словникового режиму для нас досить.
#   .venv/bin/pip install --no-deps ukrainian-word-stress
#   .venv/bin/pip install marisa-trie
ukrainian-word-stress>=2.1.0
marisa-trie>=1.1.0
