# Tasks — Interview tool

> Оцінки — **дні роботи, не календарні**, і вони попередні: після Етапу 0 їх треба
> перечитати. Профіль усюди `tool`.
>
> **Логіка порядку:** спочатку вбиваємо два найбільші ризики (чи взагалі працює
> український голос; чи вміє модель не бути навідною) і тільки потім будуємо тул.
> Зворотний порядок — це два тижні коду навколо гіпотези, яка може не підтвердитись.

## Етап 6 — Справжнє дослідження: планування подорожей (21.08.2026)

| ID | Задача | Статус | Пріоритет | Залежить від | Примітка |
|---|---|---|---|---|---|
| T-20 | Простір і гайд із документа дослідниці | **done** | high | — | `spaces/travel` + `guides/group-trips`. Формулювання питань перенесені дослівно; тривалості з гайда переведені в ліміти реплік |
| T-21 | Рушій фаз: розігрів → розповідь → карта тем → підсумок | **done** | high | T-20 | `app/interview/phases.py`. Модель даних розширена: фаза розповіді, двоуровневі питання, драбина заглиблення, питання підсумку. **25 реплік із гайда проти 10 від моделі** — якість майже не залежить від сили моделі |
| T-22 | Визначення покриття тем після розповіді | **done** | high | T-21 | Правило гайда «не питати вдруге». По темі окремо «так/ні»: одним списком слабка модель переоцінювала (9 із 10 при трьох реальних) |
| T-23 | Guard: вільне уточнення мусить бути питанням | **done** | medium | T-21 | Модель віддавала «Добре.» — це filler і оцінка, а не уточнення |
| T-25 | Дії над надиктованою відповіддю | **done** | high | T-21 | «Надіслати» доступна завжди, коли є текст; «＋ Продовжити» дописує до сказаного; «✎ Редагувати» вмикається дією, а не саме. Плюс фікс: при втраті мікрофона надиктоване переїжджає в текстове поле |
| T-26 | Тримання розповіді показується не як питання | **done** | high | T-21 | «Ага» у блоці питання виглядало дивно — і правильно: у реальній розмові це бурмотіння. Тепер тиха репліка під питанням, питання лишається на місці |
| T-27 | Аналіз відповіді одразу: закриття прогалин `must_learn` | **done** | high | T-21 | Після кожної відповіді перевіряється поточний пункт; незакритий стає фокусом наступного уточнення; тема закривається за змістом, а не за лімітом. У транскрипті `open_items` — матеріал для «Нотаток для себе» |
| T-28 | Дії видимі завжди + названі очікування | **done** | high | T-25 | Кнопки не зʼявляються, а вимикаються. Жива підказка обсягу (`interface.expected_words`, типово 15). **Поріг не блокує**: коротка відповідь валідна, інтервʼюер просить розкрити. «Редагувати або написати» активна завжди — це вхід для тих, хто волію писати |
| T-29 | Тримання розповіді без реплік | **done** | high | T-26 | «Ага» прибрано зовсім: у покроковому інтерфейсі воно неминуче читається як питання. Інтервʼюер мовчить, у транскрипті порожніх ходів немає |
| T-30 | Головна кнопка бере роль «Продовжити» | **done** | high | T-28 | Іконка + підпис `🎙 Говорити` / `🎙 Продовжити` / `⏹ Зупинити`. Окрема кнопка прибрана як дублікат |
| T-31 | Редагування на місці тексту | **done** | high | T-28 | Текст перетворюється в поле там же; кнопка під текстом, перемикається на «Готово». Виправлено дві помилки розмітки: поле опинялось під блоком дій, кнопка налізала на текст через `position: absolute` |
| T-32 | База швидкості 1,10 | **done** | medium | T-8d | «1×» звучить як 1,10 |
| T-33 | Прогрес за фазами замість «Тема 1 з 10» | **done** | high | T-29 | Прогрес читав поле, яке в режимі сценарію не рухається — тому завжди показував «Тема 1 з 10». Тепер підпис фази («Ваша розповідь — частина 3», «Уточнення: тема 2 з 10 · Де жила інформація»), лічильник питань і зважена смуга |
| T-34 | Кнопка «Я все розповіла» | **done** | high | T-29 | Без неї мовчання інтервʼюера у фазі розповіді читалось як «повторює те саме питання»: 12 ходів без змін на екрані. Автовихід (дві короткі відповіді) лишився |
| T-35 | Чекліст «хочемо почути» з галочками | **done** | high | T-27 | `must_learn` теми видимі під питанням, галочка при зарахуванні. Те саме, що вело уточнення всередині — тепер видиме респонденту |
| T-36 | Оцінка проти всієї розповіді, до 3 пунктів за хід | **done** | high | T-35 | Раніше оцінка бачила лише останню відповідь: репліка про час зараховувала пункт «хто ініціював». Плюс одна відповідь тепер може закрити кілька пунктів |
| T-37 | Перевибір ходу, якщо тема закрилась відповіддю | **done** | high | T-36 | Правило гайда «не питати вдруге»: питання, вибране до оцінки, не звучить, якщо оцінка закрила тему. Інцидент `question_skipped` у транскрипті |
| T-38 | Порядок екрана: текст і дії вгорі, доріжка при кнопці | **done** | medium | T-31 | Доріжка стосується запису, тому стоїть над кнопкою запису, а не над текстом |
| T-39 | Чекліст на кожному питанні | **done** | high | T-35 | Розігрів — `opening_expects`; розповідь — усі теми гайда з галочками по ходу; тема — `must_learn`; підсумок — `expects` питання |
| T-40 | Покриття тем визначається по ходу розповіді | **done** | high | T-39 | До 3 тем за хід замість 10 разом у кінці. **Закриває TD-26**: паузи ~10 с на переході більше немає, а чекліст заповнюється на очах |
| T-41 | Одна швидкість 1,10 без вибору | **done** | medium | T-32 | Регулятор змушував респондента ухвалювати рішення про інструмент замість думати про свій досвід |
| T-42 | Однакова іконка чекліста | **done** | medium | T-35 | `○` і `✓` мали різну ширину — список стрибав при кожній галочці. Тепер одне коло, яке заливається |
| T-43 | Швидкість 1,0 замість 1,10 | **done** | medium | T-41 | Просте рішення Марини: слухати так, як записав синтезатор. `PLAYBACK_RATE = 1.0`, підтверджено на живому відтворенні |
| T-44 | Кнопка запису більше не виїжджає під згин екрана | **done** | high | T-38, T-39 | Питання з чеклістом виросли до ~970 px при вікні 714 px, і головна дія опинилась за екраном. Екран інтервʼю розділено: питання й чекліст прокручуються, зона відповіді стоїть. `position: sticky` не годився — липкий блок лишається в потоці й накривав собою чекліст |
| T-45 | Довгий чекліст у дві колонки | **done** | medium | T-39 | Мапа з 10 тем під час вільної розповіді займала 280 px — більше, ніж саме питання. У дві колонки 167 px, вузькі екрани лишаються в одну |
| T-46 | Недоступна головна кнопка перестала виглядати активною | **done** | medium | T-28 | Синій фон на 45% прозорості читався як натиснутний. Тепер контур замість заливки — дія видима, але видно, що не працює |
| T-47 | Галочка стає, щойно людину почули — до надсилання | **done** | high | T-39 | Жива перевірка чернетки (`/api/draft`): у паузі мовлення модель оцінює сказане проти відкритих пунктів. Чернетка **не рухає рушій** — «Сказати заново» прибирає галочки разом із текстом, інакше пункт вважався б закритим при порожньому транскрипті |
| T-48 | Галочки в усіх фазах, не лише в темах | **done** | high | T-47 | Розігрів і підсумок мали чекліст, який ніколи не заповнювався: зарахування жило тільки в `topic_items_done`. Додано `opening_items_done` і `closing_items_done` |
| T-49 | «Надіслати» відкриває повний чекліст, а не довжина тексту | **done** | high | T-47 | Два винятки, обидва проти тупика: у вільній розповіді гейта немає (карта з 10 тем — мапа, прогалини гайд добирає потім), і після подвійного обсягу кнопка відкривається з поясненням «спитаю окремо» — пункт міг не зарахуватись через модель (TD-21) |
| T-50 | Кнопку «Я все розповіла» прибрано | **done** | medium | T-49 | Дублювала «Надіслати відповідь». Перехід із розповіді тепер настає сам: повний чекліст тем (не раніше ніж за `narrative_turns/3` ходів), вичерпані ходи або дві короткі відповіді |
| T-51 | Синій колір зарахованого | **done** | low | T-42 | Окремий токен `--check`, а не `--accent`: акцент задає простір, а «зараховано» мусить читатись як зараховано в будь-якому просторі |
| T-52 | Мірка для оцінювача + еталон, розмічений людиною | **done** | high | T-47 | `bin/judge_eval.py` проти `tests/data/judge_cases.json` (52 випадки, сорти hit/miss/near). Без цього «стало краще» — це відчуття, а не число. Промпти живуть в `app/interview/judge.py` і **імпортуються** міркою: копія розійшлась би з робочим кодом |
| T-53 | Формулювання питання до оцінювача: «чи можна дізнатися» | **done** | high | T-52 | 82 % → 90 % на пунктах, хибних «так» 6 → 1, усі 11 «near» відхилено. Причина: пункти гайда — це те, що ми хочемо ДІЗНАТИСЯ, а не рядок у тексті. На «чи сказав саме це» модель відповідала «ні» навіть на «Нас було шість людей» ← «скільки вас було» |
| T-54 | Жива перевірка судить тільки сказане зараз | **done** | high | T-47 | Моя помилка: до чернетки додавався весь текст інтервʼю, тому галочка ставала за старою відповіддю, поки людина говорила про інше — рівно те, що Марина побачила. Зараховане раніше не губиться: воно вже в стані рушія |
| T-55 | У розповіді чекліст підписаний «Про що вже згадали» | **done** | medium | T-53 | Там «так» означає «згадав», і рушій саме так це й використовує — відкриває тему питанням рівня 2 «до конкретики». Підпис «Хочемо почути» обіцяв більше, ніж галочка означає |
| T-56 | Контрольний набір для мірки оцінювача | **done** | high | T-52 | Написаний ПІСЛЯ підбирання формулювань, на підбір не впливав. Показав, що приріст 82 % → 95 % був підгонкою: на невидимих випадках усі формулювання дають 64-71 %, `near` 1/3. Без цього набору я б доповів фальшиву перемогу |
| T-57 | Гейт «Надіслати» прибрано | **done** | high | T-49, T-56 | Рішення Марини після того, як мірка показала точність оцінювача. Замок на судженні 64-71 % не пускав того, хто сказав усе, і пускав того, хто не сказав. Чекліст лишився підказкою; право вирішити — за людиною |
| T-58 | Критерій зарахування в пункті (`counts_if`) | **частково** | medium | T-56 | Формат правильний і готовий (`judge.item_question`), але на 4B виграшу на контролі не дає. Лишається як важіль для сильнішої моделі. У гайді ще не заповнено — заповнювати немає сенсу до T-24 |
| T-59 | Чекліст — перелік, а не чекбокси | **done** | medium | T-57 | Кружечок обіцяв «зараховано напевно», а оцінювач цього не витримує. Тепер маркер списку, почуте лише світлішає |
| T-60 | Запис голосу респондента | **done** | high | — | Дві умови, обидві обовʼязкові: `interface.record_voice` у просторі + окрема галочка згоди. Без другої — 403. «Сказати заново» видаляє файл із диска. Записи прикріплені до ходу в транскрипті, плеєри в панелі дослідника |
| T-61 | Текст згоди більше не обіцяє, що аудіо не зберігається | **done** | high | T-60 | Рядок «аудіо не зберігається» у просторі «подорожі» суперечив новій функції. Обіцянка в тексті згоди — не косметика |
| T-62 | Перший справжній запис із мікрофона | todo | high | T-60 | Тільки Марина: браузерна панель, у якій я перевіряю сторінку, доступ до мікрофона блокує. Шлях аплоуду й зберігання перевірений синтетичним блобом |
| T-63 | Одна модель на процес замість однієї на сесію | **done** | high | — | Причина двох падінь сервера за день (TD-33): `build_llm` на кожен старт і відновлення давав +3,2 ГБ у памʼяті на сесію. Перевірено: три нові сесії — приріст 0,00 ГБ (було б +9,6). Плюс замок на генерації, бо сервер багатопотоковий |
| T-64 | Конкретніший прогрес: «питання 3 з 60» і «1 з 2» на питанні | **done** | high | T-33 | «Питання 7» без другого числа не значить нічого. Плюс лічильник покриття саме цього питання в заголовку чекліста — щоб не рахувати рядки очима |
| T-65 | Повний прогін інтервʼю сценарієм | **done** | high | T-64 | `bin/run_interview.py`: заготовлені відповіді, звіт із кількістю питань на тему. Два сценарії — повні відповіді й тонкі. Це те, чим перевіряється «чи не питає зайвого», бо очима таке не видно |
| T-66 | Оцінка теми звужена до розповіді + цієї ж теми | **done** | high | T-65 | Прогін показав: відповіді на теми «Розподіл внеску» й «Гроші» закривали пункти тем «Розбіжна інформація», «Розбіжність поглядів», «Поведінка на місці» — і рушій не питав про них узагалі. **Три теми дослідження зникали молча.** Тепер оцінювач бачить лише розповідь і відповіді в межах цієї теми |
| T-67 | Відповідь підписується поточною темою | **done** | high | T-66 | `topic_id` брався зі старого `topic_index`, який у режимі сценарію не рухається — той самий корінь, що й у бага з прогресом. Через нього T-66 спершу зламав оцінку: 3 закритих пункти замість 19 |
| T-68 | Розігрів і підсумок доперепитують прогалину | **done** | high | T-48 | Скарга Марини: «В Карпати їздили» — один пункт із трьох — і рушій ішов далі. Чекліст обіцяв «хочемо почути» й обіцянки не виконував: у цих двох фазах рушій на нього **не дивився взагалі**. Тепер до 2 доуточнень у розігріві та 1 на питання підсумку |
| T-69 | Оцінка розігріву й підсумку — до вибору ходу | **done** | high | T-68 | Порядок тут не деталь: рішення «доперепитати чи йти далі» залежить від того, чи почули очікуване від питання, на яке щойно відповіли. У темах порядок протилежний, і це лишилось як було |
| T-70 | Доуточнення формулюється з гайда, не моделлю | **done** | high | T-68 | Модель на пункт «скільки вас було» спитала «Що ви робили там?», а на порожню відповідь переказала початкове питання. `gap_question` дає «А скільки вас було?» з формулювання дослідника — дослівно, без guard-а, як усі інші тексти гайда |
| T-71 | Незавершена фраза більше не зникає | **done** | high | T-47 | Скарга Марини: «коли не договорю якусь інформацію, він не відмічає те, що вже було сказано». Причина: проміжний результат розпізнавання показувався на екрані, але в `finalText` не потрапляв — обірвана фраза зникала з відповіді безслідно. Забираємо її в `onend`, а не в `stopListening`: остаточний результат часто приходить уже після stop(), і тоді хвіст дописався б двічі |
| T-72 | Правка тексту не знімає зароблених позначок | **done** | medium | T-71 | Скидання чернетки перевіряло префікс: людина виправляла початок фрази — і все зароблене зникало. Тепер міряється перетин слів (2/3), і правка позначок не чіпає, а «стерла все і сказала інше» — чіпає |
| T-73 | Прогрес розділами замість «питання 4 з 60» | **done** | high | T-64 | Число пугало й було неправдою: 60 — межа гайда, а реальне інтервʼю виходить на 15-24 питання. Плюс суцільна смуга рухалась на відсоток і нічого не означала. Тепер чотири розділи (Початок · Розповідь · Уточнення · Підсумок), пройдені приглушені, поточний акцентований і ширший |
| T-74 | «Частина 1» прибрана | **done** | high | T-73 | Питання Марини «що означає частина 1, чи буде 2» відповіді не мало: частин стільки, скільки людина захоче говорити. Це була наша внутрішня механіка на екрані. Рух у цій фазі показує чекліст тем і заповнення розділу |
| T-75 | Дві назви теми: для дослідника й для людини | **done** | high | T-39 | «Розбіжна інформація» і «Розбіжність поглядів» респондентові виглядають однаково й не означають нічого. `title` лишається картою дослідника (звіти, транскрипт, промпт моделі), `shown_as` — те, що читає людина. Порожнє `shown_as` = показуємо `title` |
| T-76 | Пункти «хочемо почути» переписані людською мовою | **done** | high | T-75 | «фактичний розподіл, а не заявлений» → «хто що робив насправді». Побічний вимірний ефект: на контрольному наборі хибних «так» стало **0 замість 2**, `near` 3/3 замість 1/3 — конкретне формулювання оцінювачу зрозуміліше. Ціна: хибних «ні» 4 замість 2, тобто зайве уточнення замість втраченого пункту |
| T-77 | Одне слово не зараховує пункт | **done** | high | T-76 | Вимога Марини: «одного слова недостатньо, людина має відповісти розгорнуто». Спільна нижня межа — 3 слова, перевірка ДО моделі: навіть якщо оцінювач скаже «так», пункт лишається відкритим. Плюс підказка «Це поки одне слово», щоб людина не гадала, чому нічого не змінилось |
| T-78 | Пункт сам каже, скільки слів йому потрібно | **done** | high | T-77 | Одна межа на всі пункти неминуче ламає одне з двох: «Нас було шість» — повна відповідь на факт, «Та посварились» — порожнеча на випадок. Тому `must_learn` тепер приймає обʼєкт `{"text": ..., "needs_detail": true}` (типово 12 слів). У гайді подорожей позначено 13 пунктів із 20 |
| T-79 | Галочки проступають одразу, а не через 4 с | **done** | high | T-47 | Зміряно: одна оцінка коштує ~1,2 с, і майже все це — обробка промпту, не генерація (кап токенів 80 → 3 нічого не змінює). Тому сервер тепер віддає відповідь після **першого** пункта, а клієнт одразу питає наступний. Перша позначка через ~1,2 с замість ~4 с; пауза до перевірки 350 мс замість 1200. Межа серії — 4 пункти за паузу, інакше 10 тем розповіді тягли б 10 викликів підряд |
| T-80 | Записи голосу переживають перезавантаження сторінки | **done** | high | T-60 | У памʼяті браузера blob-и зникають, файли на диску лишаються. Сервер віддає список записів поточної відповіді в `/api/start`, `/api/resume`, `/api/draft`, `/api/answer`; кнопка «Мій голос» видима завжди (як усі дії), вимкнена поки записувати нічого |
| T-81 | Сказане не стирається, поки питання те саме | **done** | high | T-79 | У фазі розповіді після кожного надсилання екран порожнів, і людина губила нитку. Тепер надіслане лишається приглушеним, нове пишеться поруч, а на сервер іде **лише нове** — інакше репліка подвоїлась би в транскрипті. Жива перевірка бачить усю відповідь на це питання |
| T-82 | «Редагувати» більше нічого не губить | **done** | high | T-71 | Натискання під час диктування губило останню фразу: браузер тримав її як проміжну, а редактор брав лише остаточне. Тепер редактор спершу зупиняє запис і забирає хвіст |
| T-83 | Сказане голосом і набране текстом видно окремо | **done** | high | T-82 | Відповідь стала списком шматків із походженням (`voice`/`typed`) замість одного рядка. Правка зберігає походження: недоторкане диктування лишається голосом, дописане стає текстом. Логіка винесена в `web/segments.js` чистими функціями — **інакше її нічим перевірити**, бо мікрофон у панелі заблокований |
| T-84 | Перші JS-тести | **done** | medium | T-83, TD-20 | `tests/segments.test.js`, 10 тестів під `bun test`. Часткове закриття TD-20: логіка, яку неможливо перевірити в браузері, тепер під тестами |
| T-85 | Підсвітка слів під час прослуховування + смуга часу | **done** | high | T-83 | Кожне слово знає, з якого воно запису, тому підсвічується саме те, що зараз звучить. ⚠️ Усередині запису підсвітка **пропорційна**, не за таймкодами: браузерне розпізнавання їх не дає. Тривалість беремо з годинника, бо webm від MediaRecorder часто віддає `duration = Infinity` |
| T-86 | Повернення до раніше поставленого питання | **done** | high | — | Екран «Раніше сказане»: питання, відповіді й можливість дописати. Транскрипт **тільки на дописування** — доповнення стає окремим ходом із позначкою `added_to` і показується як «згадали пізніше». Для дослідника це різні дані про память. Сценарій не рухається; тема переоцінюється |
| T-87 | Сценарний режим: темп задає людина, не модель | **done** | high | T-57, T-79 | Рішення Марини після кількох спроб полагодити оцінювача: судження про «чи можна далі» знято зовсім. Гайд дає плоский перелік питань (25 у «подорожах»), людина ходить кнопками «наступне»/«попереднє». **Модель у цьому потоці не викликається жодного разу** — перевірено тестом. Прогін: 25 питань, 26 кроків, нуль інцидентів оцінювання |
| T-88 | Чекліст став шпаргалкою | **done** | high | T-87 | Ні позначок, ні лічильника зарахованого: «0 з 10» брехало б про облік, якого немає. Заголовок «Про що варто сказати», під ним кількість пунктів. Підказка тепер про обсяг відповіді, а не про зарахування |
| T-89 | Вільний режим лишився окремим інструментом | **done** | high | T-87 | Простір без питань по темах (`example`) веде розмову моделлю — там відстеження прогалин потрібне, інакше вона не знає, про що питати. Обидва режими під тестами; межа проходить по наявності `ask_if_missed`/`ask_for_detail` у гайді |
| T-90 | Кнопка повернення з «Раніше сказане» — головна дія | **done** | low | T-86 | Була дрібною ghost-кнопкою й губилась. Стала primary зі стрілкою «←» |
| T-24 | Прогін гайда на реальних респондентах | todo | high | T-21 | Гейт: чи інтервʼю тримає 45–50 хв і чи дає матеріал для «Нотаток для себе» з гайда |

## Етап 0 — Перевірка ризиків (спайк, ~2 дні)

| ID | Задача | Статус | Пріоритет | Залежить від | Примітка |
|---|---|---|---|---|---|
| T-1 | Порівняти український STT+TTS у 2–3 провайдерів | todo | high | — | ⚠️ Лише провайдери, на які можна підписатися самостійно, без закупівель. Матеріал для тесту — **юридична лексика** (ЄРБ, ВП, ЄДРПОУ, номери справ) і жива мова, не побутові фрази. Вихід: аудіосемпли TTS «на вухо», % помилок STT, таблиця цін. **Гейт: чи голос узагалі придатний** |
| T-1a | Критерій відбору провайдера: підтримка **кастомного словника** | todo | high | T-1 | Провайдер має вміти приймати список термінів домену — інакше універсальність упирається в те, що STT калічить лексику будь-якої вузької теми. Це критерій відбору, не побажання |
| T-2 | Узгодити з ІТ доступ і білінг для **корпоративного** контура | todo | medium | T-1 | ⏸ **Не блокує Етапи 0–3** (рішення 20.08.2026: перший контур особистий, на власних ключах). Іти до ІТ уже з цифрами T-1 і робочим тулом, а не з питанням «що ви думаєте» |

## Етап 1 — Мозок інтервʼюера, текстом (~3 дні) · **у роботі, 20.08.2026**

Голос — це канал; якість інтервʼю — це промпт. Розділяємо ризики: спочатку
доводимо, що інтервʼю **змістовне й не навідне**, на дешевому текстовому каналі.

| ID | Задача | Статус | Пріоритет | Залежить від | Примітка |
|---|---|---|---|---|---|
| T-3 | Скаффолд проєкту + ядро `interview/` + текстовий канал | **done** | high | — | Структура з `architecture.md`. Адаптери провайдерів одразу, навіть з одним вендором |
| T-3a | Схема конфігу простору + валідація + референсний приклад | **done** | high | T-3 | **Робиться до промпту, не після.** Персона, мови, словник домену, приватність, брендинг, схема звіту, вендор і ключі. Приймальний тест: провести інтервʼю на сторонню тему, не торкнувшись коду |
| T-4 | Анти-навідний системний промпт інтервʼюера | **done** | high | T-3a | `interviewer.v1` + guard, який форсує правила кодом. | **Серце інструменту.** Заборони: не погоджуватись, не оцінювати, не підказувати, одне питання за раз, питати про останній конкретний випадок. Промпт версіонується в git |
| T-4a | Безкоштовний локальний мозок: MLX + gemma-3-4b | **done** | high | T-4 | Знімає блокер T-5 без жодних витрат. 2,7–3,1 с на питання, офлайн, без акаунта. Скорочений промпт (~310 ток. проти 2600) дає ×5 швидкості. Ядро питає текст, а не JSON: 4B-модель структуру ігнорує |
| T-4b | Guard: перевірка повтору питання | **done** | high | T-4a | Локальна модель повторювала те саме питання щоходу. Порівняння наборів слів, не рядків. Плюс тема подається як «напрямок», а не як текст питання |
| T-5 | Прогін на колегах + ітерації промпту | **todo** | high | T-4a | ⛔ більше не blocked: модель працює безкоштовно. Гейт: дослідник читає транскрипт і каже, чи це схоже на нормальне інтервʼю |
| T-6 | Автоперевірка на упередженість | **частково** | high | T-4 | ✅ **Живий guard готовий** (`app/interview/guard.py`): зіпсована репліка до респондента не доходить, кожне відхилення пишеться в транскрипт як `incident`. ⏳ Лишився пост-фактум прохід по готовому транскрипту — те, що guard не ловить правилами (напр. модель ставить питання дуже вузько) |

## Етап 2 — Голосовий канал (~4 дні)

> **Рішення 20.08.2026: нічого не витрачаємо, доки не вирішено питання оплати.**
> Тому голос спершу робимо на **Web Speech API браузера** — нуль витрат, нуль
> реєстрацій. Це не костиль: це базова лінія, проти якої платні провайдери в T-1
> мусять довести, що варті грошей. І перевірити її можна без LLM-ключа.

| ID | Задача | Статус | Пріоритет | Залежить від | Примітка |
|---|---|---|---|---|---|
| T-7a | Голос у браузері: Web Speech API (STT + TTS) | **done** | high | T-3 | **Нуль витрат.** Розпізнавання й синтез — на боці клієнта, сервер про вендорів не знає. ⚠️ Чесно: `SpeechRecognition` у Chrome відправляє аудіо на сервери Google — для особистого контура на нечутливих темах приймально, **для корпоративного — ні** |
| T-7c | Голосовий інтерфейс без поля введення + доріжка + живий текст | **done** | high | T-7a | `interface.mode: voice`. Мікрофон блокується під час озвучення (інакше розпізнавання чує сам інтервʼюер). Резерв у текст лишається |
| T-7d | Природність голосу + вибір голосу в панелі | **done** | high | T-7a | Різання на фрази з паузами (головний засіб), швидкість/тон/пауза в UI, список реальних голосів із прослуховуванням. ⛔ Українських голосів у браузері **один**, чоловічого немає — див. TD-10 |
| T-7b | Порівняти браузерний голос із платними (доповнення до T-1) | todo | medium | T-7a, T-1 | Якщо браузер стерпно тримає українську, платний STT/TTS може взагалі не знадобитись — це прямий мінус до кошторису |
| T-7e | Серверний конвеєр озвучення + локальний провайдер `say` | **done** | high | T-7a | `POST /api/speak` (без тексту в запиті — інакше безкоштовний TTS-проксі), відтворення в браузері, `/api/tts/voices`. Перевірено наживо: 830 КБ WAV, відтворення, блокування мікрофона під час звучання |
| T-8d | Попередній синтез + швидкість відтворення | **done** | high | T-8c | Аудіо готується у фоні одразу, натискання запускає готове (0 додаткових запитів і на повторному прослуховуванні). Швидкість `0,75× / 1× / 1,25×` через `playbackRate` — діє на льоту, висота тону зберігається |
| T-8c | Питання текстом, озвучення на вимогу | **done** | high | T-7h | `interface.autoplay: false` за замовчуванням. Одна смужка під питанням із двома станами (`▶ Прослухати` / `⏹ Зупинити`) замість трьох окремих кнопок. Прибирає очікування синтезу перед відповіддю й найгірший випадок ESPnet (12 с тишу) |
| T-8a | Банк реплік: інтервʼюер говорить записаним голосом | **done** | high | T-7c | `repertoire: bank` — модель вибирає `phrase_id`, а не формулює текст. Запис у панелі з мікрофона, віддача аудіо за id репліки. Методологічно сильніше за синтез: усі чують однакові формулювання, навідне питання неможливе за побудовою. Ліміти й покриття тем форсує ядро як раніше |
| T-8b | Захист від інтервʼю з неповним банком | **done** | high | T-8a | `/api/start` не стартує й називає прогалини (відкриття, завершення, уточнення, питання до кожної теми, перелік незаписаних). Перевірка до старту, не посеред розмови |
| T-7k | Другий локальний провайдер: ESPnet, 5 голосів | **done** | high | T-7g | Разом 8 голосів (3 Piper + 5 ESPnet), 4 з них чоловічі. Заміряно: ESPnet ~10× повільніший; придатним роблять довгоживучий воркер (20 с завантаження — один раз) і кеш аудіо (фіксовані репліки — 0,001 с). Окремий venv: `ukrainian-tts` відкатив би `ukrainian-word-stress` і зламав наголоси |
| T-7l | Заміна провайдера озвучення наживо | **done** | medium | T-7k | `TtsHolder` + `/api/admin/tts/reload`: перемикання без перезапуску, щоб не рвати незавершені інтервʼю. Старий провайдер зупиняється — інакше тримав би модель у памʼяті |
| T-7j | Прослуховування й вибір голосу в панелі | **done** | high | T-7i | Регулятори під провайдера (Piper: темп, пауза, наголоси, живість) — раніше показувались браузерні швидкість/тон, які на Piper не впливають ніяк: дослідник рухав слайдери й не чув змін. Превʼю діє на незбережених значеннях і не мутує конфіг |
| T-7i | Наголоси + нормалізація тексту для символьної моделі | **done** | high | T-7g | Виявлено: алфавіт моделі не має цифр, латиниці й великих літер — вони зникали **без помилки** («у 2019 році» → «у році»). Три шари: правило в промпті, перевірка в guard, нормалізатор із числами словами. Плюс наголоси (`U+0301`) — модель навчена їх читати |
| T-7h | Зупинка озвучення на вимогу респондента | **done** | high | T-7c | Кнопка у смужці під питанням, `Escape`, і перебивання мікрофоном. Порядок дій: глушимо голос → слухаємо (одночасно не можна — розпізнавання почує інтервʼюера) |
| T-7g | Piper: локальна нейронна модель, три українські голоси | **done** | high | T-7e | Безкоштовно, офлайн, без акаунта. `mykyta` (чол.), `tetiana`, `lada`. Синтез <1 с. **Закриває TD-10.** Побічно вирішує приватність: нічого не покидає машину |
| T-7f | Адаптер Azure Speech | **частково** | medium | T-7e | Написаний за документацією (endpoint, заголовки, SSML з екрануванням, `voices/list` замість хардкоду імен). ⛔ **На живому акаунті не перевірений** — немає ключа. Див. TD-11 |
| T-7 | Серверні адаптери `stt` (платні провайдери) | todo | medium | T-1, T-7b | Робимо, лише якщо T-7b покаже, що браузера не досить. Покроково, не дуплекс (див. `architecture.md`) |
| T-8 | Сторінка респондента: згода → інтервʼю → дякуємо | **done** | high | T-7a, T-3a | Fallback у текст, якщо мікрофон недоступний. Текст згоди — **з конфігу простору**. Корпоративне формулювання — `TODO:` юридичний департамент, **не вигадувати** |
| T-9 | Деідентифікація транскрипту (як конфігурована функція) | **done** | medium | T-3a | Маскування **на вході**: ні модель, ні диск не бачать сирого тексту. Вбудовані шаблони нейтральні (пошта, посилання, телефон, 8+ цифр); доменні формати — в конфізі простору, код їх не вигадує. Кожне спрацювання логується. Див. TD-8 про залишковий ризик |

## Етап 3 — MVP-інструмент (~8 днів)

| ID | Задача | Статус | Пріоритет | Залежить від | Примітка |
|---|---|---|---|---|---|
| T-10a | Персистентне локальне сховище + продовження інтервʼю | **done** | high | T-3 | Стан пишеться на диск після кожної репліки, `/api/resume` відновлює розмову. **Закриває TD-5**: перевірено вбивством процесу посеред інтервʼю |
| T-10 | Сховище Supabase за схемою з `architecture.md` | todo | medium | T-10a | ⏸ Потрібне, коли знадобиться доступ із кількох машин або паралельні хвилі. Для одного дослідника локального файла досить. Реюз шару доступу з `comment-layer` |
| T-11 | Конструктор гайда для дослідника | **done** | high | T-3a | Панель `/admin` (за прапорцем `--admin`): гайд, простір, транскрипти. Валідація на сервері тим самим завантажувачем, що й реальне інтервʼю — зламаний конфіг на диск не потрапляє |
| T-11a | Захист від інтервʼю текстом шаблону | **done** | high | T-11 | Новий простір створюється **чернеткою** з явними TODO замість вмісту шаблону; інтервʼю з чернетки не стартує. Знайдено приймальним тестом: новий простір «Онбординг» вітав респондента розповіддю про велосипеди |
| T-12 | Генерація посилань на інтервʼю + гейт доступу | todo | high | T-10 | Реюз публікації з паролем із `design-canvas` / `preview-auth-gate.html` |
| T-13 | Автозвіт: транскрипти → теми, цитати, болі | todo | high | T-10 | Structured output. Звіт посилається на версію промпту аналізу |
| T-14 | Програмна перевірка цитат на дослівний збіг | todo | high | T-13 | Перефразована «цитата» у звіті — це вигадані дані. Ловиться кодом, не вірою |
| T-15 | Адмінка: список інтервʼю, транскрипти, звіти, аудит доступу | **частково** | medium | T-11 | ✅ Список інтервʼю + перегляд транскрипту з інцидентами. ⏳ Звіти (T-13) і аудит доступу |

## Етап 4 — Пілот на реальному дослідженні (~4 дні + календар на респондентів)

| ID | Задача | Статус | Пріоритет | Залежить від | Примітка |
|---|---|---|---|---|---|
| T-16 | Пілот: 5–10 респондентів + **1–2 модерованих інтервʼю як контроль** | todo | high | Етап 3 | Контроль обовʼязковий: без нього ми не знаємо, чи якість зіставна з людською, і не можемо це нікому довести |
| T-17 | Калібрування промпту й гайда за результатами пілоту | todo | high | T-16 | — |

## Етап 5 — Фіксація (~1 день)

| ID | Задача | Статус | Пріоритет | Залежить від | Примітка |
|---|---|---|---|---|---|
| T-18 | Постійний дім коду + доки + техдолг | todo | medium | Етап 4 | ⚠️ рішення: окремий репозиторій на GitLab чи тека в `~/Liga Tools`. Гілка feature + Draft MR, у захищені гілки не пушимо |
| T-19 | Розгорнути **корпоративний** контур | todo | medium | T-2, T-7, TD-7, Етап 4 | Дзеркало рішення 20.08.2026: особистий контур перший, корпоративний — коли тул уже працює. Прийомка: корпоративні вимоги (згода від юристів, деідентифікація, аудит доступу) виконані |

## Закриті

| ID | Задача | Закрита | Результат |
|---|---|---|---|
